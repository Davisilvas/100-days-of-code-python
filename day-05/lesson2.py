x = 0

for n in range(0, 101): # -> it doesn't includes the last number, so in this case it stops on 100
    x += n
print(x)

y = 0
for n in range(0, 101, 2): # -> it doesn't includes the last number, so in this case it stops on 100
    y += n
print(y)
